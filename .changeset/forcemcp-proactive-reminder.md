---
"@moonshot-ai/agent-core": patch
"@moonshot-ai/kimi-code": patch
---

Make the Force MCP system reminder more directive so the model proactively uses connected MCP tools for context gathering instead of waiting for explicit user instructions.
