---
"@moonshot-ai/kimi-code": minor
---

`/probemodels` now renders a detailed panel showing each configured model's probe status, provider, model ID, capabilities, context size, and a summary of reachable/unreachable models.
