---
"@moonshot-ai/kimi-code": patch
---

Improve subagent model selection: add a `model` argument to `AgentSwarm`, expose the resolved model in `Agent` results, and clarify that `[subagent_models]` in config.toml is the default routing mechanism.
